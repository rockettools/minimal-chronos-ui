name: Docker Image CI2


on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    branjobs:

jobs:
  build:
    steps:
      - name: some docker stuff
        uses: docker/build-push-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          repository: rockettools/minimal-chronos-ui
          tags: latest
